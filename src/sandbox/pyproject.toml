[project]
name = "agent_smith_sandbox"
requires-python = ">=3.13"
version = "0.1.0"
dependencies = [
    "pydantic>=2.13.4",
]

[tool.uv]
package = true
link-mode = "copy"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
packages = ["sandbox"]